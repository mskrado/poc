services:
  webhook:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -c "pip install -q -e '.[live]' &&
             python -m voice_benchmark.webhook.server"
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
